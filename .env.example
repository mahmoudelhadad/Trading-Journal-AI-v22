VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key

# Phase 5d — SYNC_ARCHITECTURE_SPEC.md §13 Step 5 rollout gate.
# Set to the literal string "true" to enable the Sync Engine. Unset
# (or anything else) leaves it disabled — the app runs local-only,
# exactly as before Phase 5.
VITE_SYNC_ENGINE_ENABLED=false
